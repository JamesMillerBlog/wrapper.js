import { Template as auth } from "./auth";
import { Template as ethAth } from "./eth-auth";
import { Template as ethMetaverse } from "./eth-metaverse";
import { Template as webxr } from "./webxr";

export const templates = {
  auth,
  ethAth,
  ethMetaverse,
  webxr,
};

export * as types from "./types";
